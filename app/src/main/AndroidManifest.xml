<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.laroomysoft.laroomy">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />

    <application
        android:name=".ApplicationProperty"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AppHelpActivity">

        </activity>
        <activity android:name=".TimeFrameSelectorActivity">

        </activity>
        <activity android:name=".AppSettingsActivity" />
        <activity android:name=".InformationActivity" />
        <activity
            android:name=".SimpleTimeSelectorActivity"
            android:screenOrientation="nosensor" />
        <activity
            android:name=".ExtendedLevelSelectorActivity"
            android:screenOrientation="nosensor" />
        <activity
            android:name=".RGBControlActivity"
            android:screenOrientation="nosensor" />
        <activity
            android:name=".DeviceMainActivity"
            android:label="@string/DeviceMainActivity"
            android:screenOrientation="nosensor" />
        <activity
            android:name=".LoadingActivity"
            android:label="@string/LoadingActivityLabel"
            android:screenOrientation="nosensor" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>